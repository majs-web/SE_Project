<!DOCTYPE html>
<html lang="en">
    <%- include('../includes/head', {title: 'Home'}) %>
<body>
    <%- include('../includes/header') %>

<!--     <article>
        <div class="certificate">
            <div class="container">
                <h2><%= certificate.title %></h2>
                <p><em><%= new Date(certificate.date).toLocaleDateString() %></em></p>
                <p><%= certificate.body %></p>
            </div>
            <div class="link">
                <p><a href="/certificates/new">Back to all news</a></p>
            </div>
        </div>
    </article> -->

    <main>
        <h1><%= certificate.name %></h1>
        <p><%= certificate.description %></p>
        <% if(certificate.isInStock) { %> 
        <p style="color: green;">In Stock!</p>
        <% } else { %>
        <p style="color: tomato;">Sold out!</p>
        <% } %>
    </main>

    <%- include('../includes/footer') %>
</body>
</html>